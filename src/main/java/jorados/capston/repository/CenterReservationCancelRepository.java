package jorados.capston.repository;

import jorados.capston.domain.CenterReservationCancel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CenterReservationCancelRepository extends JpaRepository<CenterReservationCancel,Long> {
}
